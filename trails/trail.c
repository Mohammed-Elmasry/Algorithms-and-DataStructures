#include <stdio.h>
#include <stdlib.h>
#include "headers.h"


int main(int argc, char * argv[]){
    puts("first file");
    printf("the subtracted number is: %f\n", minus(4,5));
    printf("the multiplied numebr is: %f\n",multi(4,5));
    return 0;
}
