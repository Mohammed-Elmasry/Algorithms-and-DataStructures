#include <stdio.h>
#include <stdlib.h>

struct queue {
	int arr[6] = {};
	int head;
};

//prototypes
int dequeue(struct queue * que);
int enqueue(struct queue * que, int element);


int main(int argc, char argv[]){

	return 0;
}

int enqueue(struct queue * que, int element){

}

int dequeue(struct queue * que){

}